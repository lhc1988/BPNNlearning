select count(*) from user_data where type = 0 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' day);
select count(*) from user_data where type = 0 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '2' day);
select count(*) from user_data where type = 0 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '3' day);
select count(*) from user_data where type = 0 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '7' day);
select count(*) from user_data where type = 0 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '15' day);
select count(*) from user_data where type = 0 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' month);
select count(*) from user_data where type = 1 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' day);
select count(*) from user_data where type = 1 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '2' day);
select count(*) from user_data where type = 1 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '3' day);
select count(*) from user_data where type = 1 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '7' day);
select count(*) from user_data where type = 1 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '15' day);
select count(*) from user_data where type = 1 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' month);
select count(*) from user_data where type = 2 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' day);
select count(*) from user_data where type = 2 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '2' day);
select count(*) from user_data where type = 2 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '3' day);
select count(*) from user_data where type = 2 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '7' day);
select count(*) from user_data where type = 2 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '15' day);
select count(*) from user_data where type = 2 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' month);
select count(*) from user_data where type = 3 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' day);
select count(*) from user_data where type = 3 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '2' day);
select count(*) from user_data where type = 3 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '3' day);
select count(*) from user_data where type = 3 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '7' day);
select count(*) from user_data where type = 3 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '15' day);
select count(*) from user_data where type = 3 and user_id = ? and brand_id = ? and visit_datetime <= ? and visit_datetime > date_sub(? , interval '1' month);
